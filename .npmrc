# The private @keshavsingh3197/* packages come from GitHub Packages. PACKAGES_READ_TOKEN needs
# read:packages and must NEVER be committed — set it in your environment and as a CI secret.
@keshavsingh3197:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${PACKAGES_READ_TOKEN}
